{
  "name": "image-to-vector-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express API in front of the Python vectorization engine. Serves the POC frontend.",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "multer": "^2.3.0"
  },
  "overridesRationale": {
    "qs": "Express 5.2.1 still pins qs 6.15.3, which carries two moderate advisories (GHSA-x5fp-wj9c-mxmx, GHSA-4mjr-xmp4-gh2g). Forced to the patched line until Express bumps it; drop this override once it does."
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
